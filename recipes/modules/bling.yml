---
# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json
modules:
  - type: bling
    install:
      - dconf-update-service # a service unit that updates the dconf db on boot
      - rpmfusion
#  - type: gschema-overrides
#    include: []
# - custom-theme.gschema.override
